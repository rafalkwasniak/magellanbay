---
name: handoff-2026-08-12
description: "Sesja 12.08: tekst na FB, strona wolnej subdomeny, backup Etap 1+3 z przecwiczonym odtworzeniem, zmiana nazwy bazy na host473413_kramio, poprawki cennika. Testy 1599 -> 1631."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9b42a7e8-65de-49c5-a571-38f05836de8b
  modified: 2026-08-12T18:07:49.201Z
---

Cztery commity, testy **1599 → 1631**, wszystko odebrane przez Rafała na froncie.

| commit | co |
|---|---|
| `70f0d00` | subdomena bez sklepu zaprasza do rejestracji zamiast 404; `www` przestało być 404 |
| `8b83767` | kopie zapasowe: nocny snapshot, strażnik 36 h, stan na ekranie Ustawień |
| `ed5ce32` | dokumentacja po zmianie nazwy bazy na `host473413_kramio` |
| `11ee3c3` | landing: widać, za jaki okres jest cena („/ **rok**", „0 zł / **na zawsze**") |

## Co powstało

**Tekst na Facebooka** — pierwsza rzecz w sesji, i to od niej wziął się cały dzień: `twojanazwa.kramio.pl` z reklamy musiało zacząć odpowiadać czymś sensownym ([[plan-unclaimed-subdomain-landing]]). Zasady tonu: [[feedback-marketing-tone-kramio]].

**Backup Etap 1+3** ([[open-no-file-backups]]) z **przećwiczonym odtworzeniem** na osobnej bazie — sumy kontrolne, zdjęcia i `.env` zgodne co do bitu.

**Zmiana nazwy bazy** `host473413_shop` → `host473413_kramio`. Rafał zrobił to sam (nowa baza w panelu + eksport/import SQL), ja zweryfikowałem przed skasowaniem starej: 39 tabel, liczby wierszy co do jednego, sumy kontrolne, 39 kluczy obcych, 103 indeksy, `utf8mb4`, `AUTO_INCREMENT` — bez różnic. Dowód na to, że aplikacja pisze do nowej: `backup:run` odświeżył `last_backup_at` tylko w nowej, stara stanęła.

## Decyzje Rafała (nie podważać)

- **Cron `low` na ursalogic — WSTRZYMANY** do nowego serwera ([[disabled-ursalogic-queue-crons]]). Pomiar: 29/250 procesów, w tym 7 nakładających się workerów. Wracać tylko przy alertach.
- **Przekreślona cena na landingu — ZAMKNIĘTE** ([[open-monthly-plan-vs-struck-price]]). Landing pokazuje jawny rachunek (12 × 75, płacisz za 10), więc jest w porządku. Moja wcześniejsza ocena „UOKiK" była błędna — klientem jest przedsiębiorca, nie konsument.
- **Brak planu miesięcznego** to decyzja o KOSZCIE KSIĘGOWOŚCI, nie o technice; rozważane półrocze 450/900 ([[pricing-packages]]).

## Gotchy z tej sesji

- **`route()` i `url()` renderowane NA SUBDOMENIE budują link do tej subdomeny**, nie centrali — a tam `/rejestracja` to rejestracja klienta sklepu. Stąd `App\Support\Central::url()` ([[gotcha-route-helpers-on-subdomain]]).
- **Trzech klas nie było w buildzie**: `gap-2.5`, `break-all`, `shadow-amber-600/20`. Za to `gap-1.5` (kiedyś brakujące) już JEST — listę zawsze grepować od nowa, nie ufać starym notatkom ([[tailwind-classes-must-exist-in-build]]).
- **Konto MySQL aplikacji nie założy nowej bazy** (`ERROR 1044`) — tylko panel. Nowi użytkownicy działają na `@localhost` (gniazdo); przez `127.0.0.1` dostajesz `ERROR 1045`.
- **`diff <(...)` NIE DZIAŁA w tym shellu** — „/dev/fd/63: No such file or directory". Dwa razy dało fałszywy wynik „pliki się różnią". Porównania robić przez pliki tymczasowe.
- **Weryfikując treść strony, nie grepuj jednej formy słowa.** Szukałem „miesięcznie", a na landingu stoi „75 zł/mies." — i przez chwilę twierdziłem, że wyjaśnienia ceny nie ma. Rafał sprostował.

## Rafał sprostował cztery razy — wszystkie trafione

1. **Rama marketingowa**: Kramio to sprzedaż z koszykiem, nie „oferówka", i sklep PRODUKTÓW, nie usług (żadnych warsztatów i napraw).
2. **Język**: „klienci… składać zamówienia **same**" — błąd rodzaju i koślawa konstrukcja.
3. **Cennik**: przekreślenie już jest, a matematyka na landingu się zgadza — mój alarm był nietrafiony.
4. **Skład**: „0 zł na zawsze" wyglądało sklejone → ukośnik i `flex gap-2`, jak w kartach płatnych.

## Otwarte na jutro

Nic nie blokuje wyjścia do ludzi. Zostaje: **Etap 2 backupu** (czeka na przeprowadzkę), **przegląd prawnika** dokumentów, **test produkcyjny InPost** (czeka na sprzedawcę z własnym kontem InPost), **przeprowadzka na nowy serwer**. Oraz post na FB — jedyna pozycja, która może przyprowadzić pierwszego prawdziwego sprzedawcę.

**Rano warto zerknąć:** pierwsza automatyczna kopia leci o 3:00 z nowej bazy. Cisza strażnika o 9:00 = wszystko gra.
