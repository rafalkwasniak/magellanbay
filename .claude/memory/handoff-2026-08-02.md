---
name: handoff-2026-08-02
description: "Handoff 02.08.2026: karta sklepu na Facebooka + odzyskiwanie hasła + zgoda na ciasteczka. 9 commitów, testy 1236 → 1277. NASTĘPNA SESJA = WYSYŁKI (ShipX/Furgonetka)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5c4f5743-5e78-4788-bcf6-2be5ad5638e4
  modified: 2026-08-02T19:44:52.379Z
---

Dziewięć commitów, testy **1236 → 1277**, wszystko zweryfikowane na produkcji (nie tylko w suicie).

## 1. Karta sklepu do social mediów (`4c08830`, `41c3f50`)

Moduł kompletny — szczegóły i decyzje w [[plan-shop-og-image-redesign]]. Skrót: scena z monitorem, w którym zdjęcia produktów wpasowane PERSPEKTYWICZNIE (Imagick), gradient z palety motywu, napisy z GD/Imagick. Zero AI w runtime, JPG ~130 KB, ~0,9 s.

Decyzje Rafała domknięte: logo takie, jakie wgrał sprzedawca; **bez przycisku „Odśwież grafikę"** w panelu; `kramio.pl` zostaje przy swojej grafice.

## 2. Odzyskiwanie hasła (`65cdcc6`) — funkcji NIE BYŁO w ogóle

Dwie ścieżki, bo architektura kont jest różna:
- **Sprzedawca w centrali:** token brokera `users`, ważny GODZINĘ (krócej niż 24 h aktywacji — hasło odzyskuje się od razu, więc krótszy link to mniejsze okno). Trasy `/odzyskiwanie-hasla`, `/nowe-haslo/{token}`.
- **Klient sklepu:** PODPISANY link z identyfikatorem konta, bo konta są per sklep i ten sam e-mail bywa kontem u wielu sprzedawców — broker po samym adresie trafiłby w cudze. Trasy `/nie-pamietam-hasla`, `/nowe-haslo/{customer}` (middleware `signed` + sprawdzenie `shop_id`).

**Odpowiedź IDENTYCZNA dla adresu istniejącego i nieistniejącego** — inaczej formularz byłby wyszukiwarką kont. Konto nieaktywowane dostaje mail AKTYWACYJNY zamiast resetu (z zewnątrz nierozróżnialne). Limit 3/min per IP (`security.public_forms.password_reset`). Nowe hasło przechodzi te same reguły co rejestracja; zmiana odświeża `remember_token`, więc wylogowuje obce urządzenia.

## 3. Zgoda na ciasteczka (`e2d8130`, `073e7e9`, `95674ba`, `74535c0`, `9dc0d72`, `ae7459d`)

Temat zamknięty — pełny opis w [[plan-cookie-consent]]. Sedno: **blokada SERWEROWA** w komponencie `x-google-analytics` (bez zgody skrypt nie trafia do HTML), baner w kolorach motywu przez zmienne CSS, zgoda izolowana per host (`SESSION_DOMAIN` pusty), decyzja **tylko w ciasteczku** — bez bazy (decyzja Rafała: ruch anonimowy, wiersz na gościa to koszt bez pożytku).

**Pytamy ZAWSZE, nie tylko gdy GA włączone** (propozycja Rafała): zgoda z zapasem nic nie kosztuje, a jej brak w dniu, gdy dojdzie piksel czy mapa, to przerabianie mechanizmu u wszystkich naraz.

Sześć commitów, bo Rafał wyłapał po kolei: brak linku na centrali → brak w panelu → link kasował decyzję bez pokazania banera (droga w jedną stronę) → brak potwierdzenia na ekranie → pozycja w menu zamiast pod treścią. **Wniosek: przy funkcji dotykającej wielu layoutów przejść je WSZYSTKIE od razu — panel, guest, public, landing, storefront — zamiast czekać na zgłoszenia.**

## GOTCHY z tej sesji — WARTE ZAPAMIĘTANIA

1. **`users.password` jest NOT NULL, a rejestracja wstawia LOSOWY CIĄG ZASTĘPCZY** (`Str::password(32)`). Sprawdzanie „konto bez hasła" przez `password === null` daje ZAWSZE fałszywy wynik. Jedyny wiarygodny znacznik aktywacji to `email_verified_at` → stąd `User::isActivated()`, symetryczne do `Customer::isActivated()`. Złapał to test, nie przegląd kodu.
2. **Generowanie karty musi być zablokowane w testach.** `ShopObserver` zleca ją przy KAŻDYM `Shop::factory()`, kolejka testowa jest synchroniczna, składanie trwa ~0,9 s → suita rosła z 44 s do **ponad 10 minut**. Blokada w `TestCase` (`Bus::fake([GenerateShopOgImage::class])`). Nie zdejmować.
3. **Test wrażliwy na datę, popsuty od dawna:** `BulkMailingPanelTest` szukał frazy „Wysłano 0", a obok stoi data wysyłki („Wysłano 02.08.2026"). Przewracał się przez pierwsze DZIEWIĘĆ DNI każdego miesiąca; nie wyszło wcześniej, bo pracowaliśmy pod koniec lipca. Lekcja: `assertDontSee` na prefiksie łapie przypadkowe podciągi — używać pełnych fraz.
4. **`config()` NIE DZIAŁA w `bootstrap/app.php`** — plik wykonuje się przed wczytaniem konfiguracji, więc wywołanie kończy się BIAŁYM EKRANEM (produkcja zwracała 500), nie ostrzeżeniem. Wartości wpisywać wprost, a zgodność z configiem pilnować testem.
5. **Pełna suita puszczana w kółko ZDUSIŁA CRONY CAŁEGO KONTA** (18:15–18:17): `Fork failed`, nie wystartowały `email:dispatch` ani `queue:work`, alert na Discordzie. Zawiesiła też samego asystenta — jego komendy też nie miały na czym wystartować. Szkód brak (kolejka pusta, zero failed_jobs). **W trakcie pracy testy FILTROWANE, pełna suita raz przed commitem** → [[open-hosting-process-limit]].
6. **Uwaga przy testowaniu na produkcji:** wywołanie mailera wrzuca REALNY wpis do outboxu i cron go wyśle. Trzeba go skasować albo od razu testować w suicie.

## Metoda warta powtórzenia

Przy grafice Rafał decydował **oglądając makiety**, nie czytając opisy. Kolejne warianty składane ImageMagickiem i publikowane pod `kramio.pl/images/podglad/` (katalog potem skasowany) skróciły dyskusję z godzin do kilku wymian zdań — pierwszy dylemat („za konkretne czy za ogólne") rozstrzygnął w dziesięć sekund, patrząc na obrazki.

Drugi wniosek: **jego decyzje o tym, czego NIE robić, wymuszały poprawki w kodzie.** Rezygnacja z przycisku odświeżania odsłoniła, że skrót w nazwie pliku nie reagował na podmianę zdjęcia — bez przycisku sprzedawca nie miałby jak tego naprawić.

## NASTĘPNA SESJA

Cookies ZROBIONE. Następne: **wysyłki** (etykiety ShipX, Furgonetka, token geowidgetu) → **panel admina** → [[priorities-launch-first]].
