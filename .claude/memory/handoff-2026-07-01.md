---
name: handoff-2026-07-01
description: "Handoff 2026-07-01 — dane do przelewu, fundament pakietów (snapshot), dopieszczenie panelu sprzedawcy, inteligentne usuwanie produktu, tagi, limit zdjęć. SSL subdomen działa."
metadata: 
  node_type: memory
  type: project
  originSessionId: fa4db0ba-efa6-4709-8c45-c76fa2098faf
---

Sesja 2026-07-01 (długa, wszystko na main, pushnięte). Stan testów: **182/182 zielone**.

## Wdrożone dziś (kolejno)
1. **Dane do przelewu + metoda „przelew na konto"** ([[bank-transfer-payment-method]], commit e8c67bb) — trójpodział dana/metoda/integracja.
2. **Fundament pakietów — model SNAPSHOT** ([[plan-packages]], commit cb8bc5b): `config/shop.php` definiuje 3 tiery (slug `stall`/`booth`/`pavilion` → nazwy Kram/Stragan/Pawilon); `Shop::assignPackage()` (snapshot), `entitlement()` (resolver: snapshot ?? config-fallback), `packageName()`. Kolumny na `shops`: `package`, `entitlements` (JSON), `subscription_ends_at`, `comped`. ProductController pyta resolver zamiast zahardkodowanego `packages.free`. Oba istniejące sklepy dostały snapshot `stall`.
3. **Dopieszczenie panelu sprzedawcy** (commit dc33d05):
   - Spójność pakietu w UI (Kram, nie „Free") + kafelek pakietu na pulpicie.
   - Numer konta formatowany po polsku (2+grupy po 4).
   - **Nawigacja mobilna**: hamburger + drawer (`resources/js/mobile-nav.js`, `<x-panel-nav-items>`); wcześniej poniżej lg NIE było menu.
   - Martwe linki menu (np. Zamówienia) → nieklikalne „wkrótce".
   - **Inteligentne usuwanie produktu**: niezamówiony → `Product::purge()` (trwałe: pliki zdjęć z dysku + kaskada FK); zamówiony → soft-delete. `Product::hasBeenOrdered()` teraz `return false` — **DO ZMIANY z modułem zamówień** na `orderItems()->exists()`. Pliki zdjęć kasuje hook `ProductImage::deleting`.
   - **Tagi**: podpowiedzi wg popularności (tylko z ≥1 produktem); osierocone sprzątane przy zapisie/usuwaniu (`Shop::pruneOrphanTags`). Wyczyszczono 3 osierocone tagi w bazie.
   - **Limit zdjęcia 4→20 MB** (`config/shop.php` `product_images.max_upload_kb`); i tak skalujemy do WebP. Fix z-index dropdownu tagów.

## Ważne haczyki na przyszłość
- **`Product::hasBeenOrdered()` = false** dopóki nie ma zamówień → każde usunięcie jest teraz TWARDE. Gałąź soft-delete ożyje sama po zmianie tej jednej metody.
- **SSL subdomen `*.kramio.pl` DZIAŁA** ([[front-blocked-on-subdomain-ssl]]) — storefront i „Wygląd" (kolory/szablony) są odblokowane, można robić.

## W kolejce (nie zaczęte)
- Moduł **Zamówień** (odblokuje soft-delete, płatności/metody w kasie, licznik numeracji).
- **Storefront** na subdomenie (teraz możliwy) + kolory/szablony ([[storefront-theme-system]], [[storefront-draft-preview]]).
- **Pakiety — wykonanie dalej**: edytor uprawnień per sklep w adminie (konsola), miękki zamek przy zejściu, billing/odnowienia. Kwoty i finalne decyzje otwarte.

Poprzedni handoff: [[handoff-2026-06-30]].
