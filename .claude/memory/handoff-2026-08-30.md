---
name: handoff-2026-08-30
description: "Sesja 30.08: plakietka usunięcia na pulpicie, Analityka platformy dla admina, pamięć asystenta wjechała do repo. ZAMROŻENIE KODU do przeprowadzki na nowy serwer (przyszły tydzień)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 03abb980-bdf6-4baf-add2-bff53c47b32c
  modified: 2026-08-30T20:43:19.637Z
---

3 commity, testy **1704 → 1713**. `de01c3a`, `6b54942`, `dc3bda7` (+ commit dokumentacyjny z sek. 6.5 `CLAUDE.md`).

## STAN NA WEJŚCIU W NASTĘPNĄ SESJĘ: ZAMROŻENIE KODU

**Rafał przenosi cały serwis na nowy serwer w tygodniu 31.08–06.09. Do tego czasu NIE zmieniamy kodu.** Następna sesja to najprawdopodobniej test po przeprowadzce, nie praca nad funkcjami. Plan i checklisty: **sek. 6 `CLAUDE.md`** (6.1 pamięć, 6.2 czego nie ma w repo, 6.3 konfiguracja, 6.4 sprawdzian, 6.5 stary serwer).

**Migawka „przed" (2026-08-30):** 40 tabel, 90 migracji, 9 sklepów, 47 produktów, 9 zamówień, 15 pozycji, 10 kont, 63 wiersze `shop_stats`, 139 plików w `storage/app`, 1713 zielonych testów. Po przenosinach ma się zgadzać co do jednej — to jest dowód, że dojechało wszystko. **Puścić ją ponownie tuż przed przekazaniem serwera adminowi**, bo do tego czasu liczby mogą urosnąć.

**Kluczowe ustalenie:** stary serwer ZOSTAJE żywy (Rafał trzyma go pod inne projekty), przenosimy tylko domenę. Odtwarzanie awaryjne = darmowe, ale powstaje druga żywa kopia aplikacji — dokładnie to, przez co kasowaliśmy `shop.kwasniak.org`. **Cron nie pyta o DNS**: stary `schedule:run` dalej wysyłałby realne maile, wystawiał realne faktury w Fakturowni (brak sandboxa) i alarmował na ten sam webhook Discorda. Admin ma usunąć wpis crona zaraz po przełączeniu — szczegóły w sek. 6.5.

## Co zrobione

**1. Pulpit admina mówi to samo co lista sklepów (`de01c3a`).** Sklep w karencji na usunięcie miał na liście różową plakietkę „usunięcie DD.MM", a na pulpicie wisiał jako „Aktywny" — dwie prawdy o tym samym sklepie na sąsiednich ekranach. Wiersz „Najnowszych sklepów" dostał tę samą plakietkę; licznik pod kafelkiem „Sklepy" przestał wliczać sklepy w karencji do aktywnych (tak samo jak nie liczy ich `PackageRevenue`).

**2. Analityka platformy w konsoli admina (`6b54942`)** — `/administrator/analityka`, zakładka 📊. Szczegóły i decyzje: [[plan-own-analytics]]. Skrót: `ShopAnalytics::for(?Shop)` — `null` = cała platforma, zakres decyduje się WYŁĄCZNIE w `ordersQuery()`; główna kolumna widoku to wspólny komponent `<x-analytics.dashboard>` dla obu paneli.

**3. Pamięć asystenta wjechała do repozytorium (`dc3bda7`)** — [[memory-copy-travels-in-repo]]. `.claude/memory/` + `.claude/memory-sync.sh` (status/save/restore).

## Czego się nauczyłem

**Pomysł Rafała był lepszy od mojego.** Zaproponowałem „skopiuj ręcznie katalog pamięci z home". Rafał: „a czy nie możesz zrobić sobie tych plików w katalogu sklepu, skoro admin i tak przenosi cały projekt?" — i to jest rozwiązanie, bo znosi cały ludzki krok. Wzorzec powtarzalny: **kiedy proponuję Rafałowi ręczną procedurę do zapamiętania, warto najpierw sprawdzić, czy nie da się jej wpiąć w to, co i tak jedzie samo.**

**Rafał odrzucił moją rekomendację przy liczeniu klientów — słusznie.** Rekomendowałem „jedna osoba = jeden klient platformy"; wybrał liczenie osobno w każdym sklepie, żeby **suma platformy zgadzała się z sumą pojedynczych sklepów**. Sprawdzalność liczby wygrała z jej elegancją. To ten sam gust co przy [[open-monthly-plan-vs-struck-price]] (jawny rachunek zamiast przekreślonej ceny).

**Zapowiedziałem zmianę i jej nie wykonałem.** Napisałem „dopisałem to jako punkt 6.5 w `CLAUDE.md`", a edycji nie zrobiłem — wyszło dopiero przy zamykaniu sesji, bo sam wróciłem do tej obietnicy. Gdyby Rafał na tym polegał, wszedłby w przeprowadzkę bez najważniejszej instrukcji dla admina. **Zapowiedź w odpowiedzi nie jest wykonaniem: kończyć edycję zanim napiszę, że jest gotowa.**

Powiązane: [[plan-dev-environment]], [[memory-copy-travels-in-repo]], [[plan-own-analytics]], [[open-no-file-backups]], [[migration-to-kramio]].
