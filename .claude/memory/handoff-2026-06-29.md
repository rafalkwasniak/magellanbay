---
name: handoff-2026-06-29
description: "Stan na koniec 2026-06-29. Panel sprzedawcy mocno rozbudowany (sklep/produkty/wygląd/ustawienia/profil/pulpit). Wszystko na main, 143 testy zielone. Następne: storefront LUB Integracje LUB Zamówienia."
metadata: 
  node_type: memory
  type: project
  originSessionId: 165038ad-efc3-43c1-8971-974bdeca5df0
---

Stan na koniec **2026-06-29**. Working tree czysty, wszystko na `main`, **143 testy zielone**. Tryb pracy: element po elemencie + przystanek na froncie + „CP" = commit&push ([[incremental-checkpoints-per-element]], [[cp-shorthand-commit-push]]).

## Zrobione w tej sesji (zacommitowane na main)
- **Pulpit sprzedawcy** — checklista przebudowana w **uporządkowaną ścieżkę do publikacji** (5 kroków: Dane sklepu → Dane firmowe → Opis → Logo → Pierwszy produkt, ten ostatni „publikuje"). Każdy krok deep-linkuje do konkretnej sekcji (kotwice `#adres`/`#dane-firmowe`/`#dane-podstawowe` na `shop.edit`, `#logo` na `appearance.edit`, `scroll-mt-24`). Statystyka „Produkty" = realna liczba; box „Co dalej?" dynamiczny. Krok liczony z danych w `DashboardController`.
- **Produkty** — lista jako **siatka kafelków** (układ 8/12+4/12, 2/3/4 kolumny responsywnie, dolny blok cena+stan+ikonki przyklejony `mt-auto`), **paginacja** `paginate(12)`, `page` przechodzi przez cały cykl edycji (linki listy + „Wróć do listy" + redirect po zapisie). Zdjęcia: limit **8**, siatka 4, zapis **WebP** (`config('shop.product_images')`), po zapisie wracamy do edycji. Limit Free = **24** (dzieli się równo).
- **Wygląd** (`seller.appearance.*`, `/sprzedawca/wyglad`) — logo wydzielone z „Mój sklep" na osobną stronę w lewym menu; placeholder „Kolory i szablon — wkrótce".
- **Ustawienia** (`seller.settings.*`, `/sprzedawca/ustawienia`) — domyślny VAT (kolumna `default_vat_rate` na `shops`, default '23'); formularz nowego produktu prefilluje się nią.
- **Profil** — avatar wydzielony do osobnego boxa pod „Dane osobowe".
- **Drobiazgi** — paginacja po polsku (`lang/pl.json`: Showing/to/of/results); toast 30s→**15s** + krótki „shake" przy pojawieniu (`.toast-attention`, respektuje prefers-reduced-motion).

## Stan paneli
Lewe menu sprzedawcy: **Pulpit · Mój sklep · Produkty · Zamówienia(stub) · Wygląd · Ustawienia**. Wszystkie poza „Zamówienia" działają. Sklep ma status Draft/Active, ale **auto-publikacja po 1. produkcie NIE jest jeszcze zaimplementowana** (komunikat na pulpicie/„Twój sklep" to obietnica, brak kodu przełączającego status na Active).

## W PLANIE (nic nie zaczęte)
1. **Integracje** (#4 z rozbicia menu) — pozycja w lewym menu + tabela `shop_integrations` (typ/enabled/zaszyfrowany JSON: GA, PayU, InPost…). Patrz [[plan-shop-settings-storage]] kat. 3. Świadomie odłożone jako osobny split.
2. **Wygląd — kolory/szablony** — rozbudowa strony „Wygląd" (box-placeholder już jest). Wiąże się z [[storefront-theme-system]].
3. **Storefront** (subdomena `{slug}.kramio.pl`) — Blade-first, podgląd szkicu dla właściciela ([[storefront-draft-preview]]), `Route::domain` to szkielet w `routes/web.php`. Duży, przyszły temat. Wildcard DNS jest, vhost+SSL = działka Rafała.
4. **Zamówienia** — pozycja w menu to stub; moduł do zbudowania (numeracja per-sklep od 1, outbox jako historia — [[email-outbox-cron-pattern]]).
5. ~~**Auto-publikacja sklepu**~~ — **ZROBIONE 2026-06-30** (status sklepu = widoczność, napędzany aktywnymi produktami w obie strony przez ProductObserver; commit f60e315) — [[shop-visibility-auto-publish]].
6. **Strony informacyjne sklepu** (regulamin, O nas, dostawa) — własne tabele, później ([[plan-shop-settings-storage]] kat. 4).

## Uwagi techniczne
- PHP 8.5: `/opt/alt/php85/usr/bin/php` (artisan, composer, testy).
- **Build** pod obciążeniem hosta (LVE) pada na EAGAIN — komenda, która dziś dowiozła: `RAYON_NUM_THREADS=1 TOKIO_WORKER_THREADS=1 UV_THREADPOOL_SIZE=1 npm run build`. Szczegóły i kolejność prób: [[vite-build-rayon-threads]].
- Konto testowe Rafała: rafal@kwasniak.org (seller).
- DeepSeek („Popraw przez AI") działa po wklejeniu `DEEPSEEK_API_KEY` (teraz pusty) — [[deepseek-ai-improve]]. **Klucz GUS REGON: wpięty własny PRODUKCYJNY klucz Rafała (2026-06-30), zweryfikowany live — auto-fill z NIP działa dokładnie** — [[plan-nip-autofill-and-editor]].
