---
name: handoff-2026-07-25
description: "Handoff 2026-07-25 (z Michałem): cały PANEL kodów rabatowych (KR-A…KR-E + poprawki UX), zwroty Faza A (pouczenie 14 dni), sprzątaczka komentarzy. 844 testy. JUTRO RANO: front kodów rabatowych."
metadata: 
  node_type: memory
  type: project
  originSessionId: 37f72f3b-963d-48a4-ba3e-06ca036f5d60
  modified: 2026-07-25T20:51:55.742Z
---

**Sesja 2026-07-25 (z Michałem). Wszystko na `main`, wypchnięte, 844 testy zielone.**
Ostatnie commity: `343436d` (panel kodów), `d137332` (kod dla klienta), `b85711f` (zwroty Faza A), `33a1946` (sprzątaczka).

## 1. Kody rabatowe — PANEL KOMPLETNY (główny temat dnia)
Pełne ustalenia, decyzje i stan kodu: [[plan-discount-codes]]. Skrót: 3 typy (procent / kwota / darmowa wysyłka), 2 zakresy (koszyk / produkt), opcjonalne ograniczenia (próg, termin, limit użyć, kod imienny). Bramka `discount_codes` = Pawilon, egzekwowana realnie (403/404).

Zbudowane w 5 checkpointach + 4 rundy poprawek od Michała: filtry przeniesione do prawej kolumny (wzorzec z Analityki), wyszukiwarka (kod / nazwa produktu / imię, nazwisko, e-mail klienta), stronicowanie po 10, akcje wiersza jako przyciski (styl z Informacji), powrót po zapisie na TĘ SAMĄ stronę listy.

**Dane testowe:** sklep `ilikemybike` ma **51 kodów** (50 zasianych skryptem + 1 ręczny) w różnych stanach: 31 aktywnych, 9 wygasłych, 6 zaplanowanych, 5 wyłączonych; 9 na produkt, 8 imiennych, 18 z progiem. Skryptu zasiewu NIE ma w repo (był w scratchpadzie sesji) — gdyby był potrzebny znowu, trzeba go napisać na nowo. Stanu „Wyczerpany" nie widać, bo powstaje dopiero, gdy zamówienia zaczną używać kodów (czyli po froncie).

## 2. Zwroty — Faza A WDROŻONA
Obowiązek informacyjny (14 dni) spełniony: flaga art. 38 per produkt, terminy na zamówieniu, pouczenie + wzór oświadczenia w mailu potwierdzającym. Szczegóły i rozstrzygnięcia w [[legal-consumer-returns-withdrawal]]. Zostają Fazy B i C.

## 3. Sprzątaczka
Komentarze w `routes/web.php`, `config/tenancy.php` i `CLAUDE.md` twierdziły, że storefront na subdomenach jest wyłączony — nieprawda usunięta ([[front-blocked-on-subdomain-ssl]]). Przy okazji trzy opisy tras panelu opisujące stan sprzed miesiąca.

## JUTRO RANO (2026-07-26): front kodów rabatowych
Ustalone z Michałem na koniec sesji. Kolejność z [[plan-discount-codes]]:
1. Pole „Masz kod rabatowy?" w koszyku (Livewire `Cart`) + walidacja: stan kodu, próg `min_items_total`, zakres (koszyk/produkt), kod imienny vs zalogowany klient, limit użyć.
2. Przeniesienie rabatu na zamówienie w `OrderService` — migawka `discount_code` + `discount_amount`; typ „darmowa wysyłka" zeruje `delivery_cost` (NIE zapisuje się w `discount_amount`).
3. **Rozbicie rabatu proporcjonalnie na pozycje i ich stawki VAT** — jedyny nietrywialny kawałek matematyki; MUSI wejść razem z frontem, bo faktury idą przez Fakturownię ([[plan-fakturownia]]).
4. Rabat widoczny w podsumowaniu kasy, mailach i na koncie klienta.

Tryb pracy sprawdzony w tej sesji i wart powtórzenia: małe checkpointy z przystankiem na froncie po każdym ([[incremental-checkpoints-per-element]]), testy przy każdym kroku, klasy Tailwinda sprawdzane w buildzie PRZED użyciem ([[tailwind-classes-must-exist-in-build]] — `max-w-xs`, `pr-16`, `gap-x-4`, `md:col-start-2`, `tracking-widest` NIE istnieją).
