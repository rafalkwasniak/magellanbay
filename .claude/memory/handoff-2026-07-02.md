---
name: handoff-2026-07-02
description: Handoff 2026-07-02 — motyw sklepu (picker w panelu) + limit 6 promocji na głównej; 197 testów; storefront render dalej TODO.
metadata: 
  node_type: memory
  type: project
  originSessionId: 5dd13c1f-9d32-4910-9b9a-5587fda156a6
---

> **Sesja 2 tego samego dnia (kontynuacja, po przerwie na limity).** Domknięta warstwa filtrów wykazu — commit `f46e35a`, **235 testów zielonych**. Na wykazie: sortowanie (4 opcje), filtr tagów **AND** + chmura po popularności, powrót z produktu na tę samą przefiltrowaną stronę (`?powrot=` z walidacją lokalnej ścieżki). Wszystkie 3 wymogi Rafała spełnione (reset strony do 1 / paginacja niesie filtry / powrót z filtrami). Szczegóły w [[plan-storefront-theming]]. Po drodze: wcześniejszy commit `58e6a5d` (finisz warstwy przeglądania: `.st-card` + HomeProductsTest) oraz jednorazowy UPDATE cen produktów w DB na losowe 10000–35000 zł (test danych do sortowania). **NASTĘPNE:** koszyk → kasa → konto klienta → panel Zamówień.

**Sesja 2026-07-02 (z Rafałem).** Dwa domknięte etapy, oba wypchnięte, 197 testów zielonych.

## Zrobione
1. **Warstwa motywu sklepu — połowa A (wybór w panelu).** Commit `49c6a28`. Ustalona cała architektura (3 osie: szablon/układ/adaptacja, bloki, render przez zmienne CSS w :root) — szczegóły w [[plan-storefront-theming]]. W kodzie: `config/themes.php` (3 szablony: Aksamitna chmurka/Zielony zakątek/Grafitowy wieczór, po 2 palety, kontrakt 4 tokenów), kolumny `shops.template`+`shops.theme` (prod DB zmigrowana), resolver na modelu, picker w zakładce „Wygląd" z żywym mini-podglądem witryny (losowe realne zdjęcie produktu, gdy jest; inaczej placeholder w kolorze palety).
2. **Limit 6 produktów promowanych na głównej.** Commit `a39860d`. Wykorzystane istniejące `products.show_on_homepage`; sufit `config('shop.homepage_promoted_limit')`; egzekwowanie w `ProductRequest::withValidator()`; UX „Zajęte X z 6 miejsc" na formularzu produktu.

## Ustalenia produktowe z tej sesji
- Kolory szablonów = na razie rozsądny placeholder; **Rafał dopyta projektanta** o palety pod nazwy — dopracujemy później, nie teraz.
- E-booki vs inne produkty — NIE rozróżniamy w v1 (za dużo decyzji naraz).
- Główna ma być „wow", nie drugi listing — stąd limit 6.

## Następny etap (do wyboru jutro)
- **Storefront — pierwszy render motywu** (połowa B): subdomena `{shop}.kramio.pl` + middleware `ResolveShop` + minimalna omotywowana strona (paleta jako :root CSS vars, tożsamość sklepu, bramka „już wkrótce" dla szkicu — [[storefront-draft-preview]]). To prawdziwy unlock (dzisiejszy motyw NAPRAWDĘ się renderuje), ale wchodzi w tenancy — większy blok czasu. Szkielet `Route::domain` czeka zakomentowany w routes/web.php; SSL/subdomeny działają ([[front-blocked-on-subdomain-ssl]]).
- Potem: bloki + przepis głównej z adaptacją do liczby (0/1/kilka/6) renderujący wyróżnione produkty.

Powiązane: [[plan-storefront-theming]], [[handoff-2026-07-01]], [[incremental-checkpoints-per-element]], [[multitenant-subdomain-architecture]].
