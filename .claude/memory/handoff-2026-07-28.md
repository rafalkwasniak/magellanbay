---
name: handoff-2026-07-28
description: "Handoff 2026-07-28: SEO ZAMKNIĘTE w całości (box + opisy od AI + strony tekstowe) oraz limity użyć AI WDROŻONE i przetestowane na żywo. 976 testów. Następny temat: Zwroty Fazy B/C."
metadata: 
  node_type: memory
  type: project
  originSessionId: 37f72f3b-963d-48a4-ba3e-06ca036f5d60
  modified: 2026-07-28T08:19:30.557Z
---

**Sesja 2026-07-28 (z Michałem). Trzy commity na `main`, wypchnięte, 976 testów zielonych.**
`769acd5` box SEO · `59bc39b` opisy od AI · `2ee6cef` strony tekstowe + fallback produktu · `0cc024b` limity AI.

## 1. SEO — TEMAT ZAMKNIĘTY
Wszystkie punkty audytu ursalogic z kategorii „oczywiste" gotowe; stan i to, czego świadomie NIE zrobiliśmy (dane strukturalne, LCP, CSP), w [[plan-seo-audit]]. Dziś doszły: box „SEO" w formularzu produktu i sklepu, opisy pisane przez AI (job + przycisk), opis dla stron tekstowych (WYŁĄCZNIE ręczny — bez AI, decyzja Rafała: Regulamin i PP są długie i niewyszukiwane), hybrydowy fallback produktu.

## 2. Limity użyć AI — WDROŻONE I PRZETESTOWANE NA ŻYWO
Pełny opis w [[plan-ai-usage-limits]]. Skrót: tygodniowa pula ZADAŃ per pakiet (100/400/800, okno ISO), egzekwowana w `AiClient::run()` ze sklepem jako obowiązkowym argumentem, licznik widoczny przy każdym przycisku i zbijany natychmiast.
**Kluczowa obserwacja: pomiar zmienił projekt.** Wywołanie kosztuje 0,05–0,7 grosza, więc limit nie chroni budżetu przed sprzedawcą, tylko przed skryptem (throttle 30/min ≈ 43 tys. wywołań dziennie ≈ 60 zł). Dlatego liczby są hojne — ostrożne 20–30 tygodniowo przeszkadzałoby przy zakładaniu sklepu bez żadnego zysku.
Rafał przetestował na `ilikemybike` (symulacja Stragan + limit 5), po czym **stan przywrócony do Pawilonu** z ręcznymi nadaniami nietkniętymi.

## 3. Rzeczy warte zapamiętania z tej sesji
- **Licznik AI i `window.setAiQuota()` to skrypt INLINE w komponencie Blade**, nie paczka Vite — kolejne zmiany w tym obszarze NIE wymagają builda. Dziś trafiły się dwa buildy, bo ruszaliśmy `resources/js/ai.js` (`task_id`, wariant dymka, wywołanie `setAiQuota`).
- Build robi Rafał z terminala: `cd /home/host473413/domains/kramio.pl && RAYON_NUM_THREADS=1 npm run build`. Weryfikacja: `grep -c "setAiQuota" public/build/assets/app-*.js`.
- Klasy Tailwinda nadal trzeba sprawdzać w buildzie PRZED użyciem ([[tailwind-classes-must-exist-in-build]]) — `max-w-xs`, `pr-16`, `gap-x-4`, `tracking-widest` DALEJ nie istnieją.
- Wyczerpana pula to INFORMACJA, nie błąd — bursztyn, nie czerwień. Rafał wyłapał, że komunikat „wygląda walidacyjnie".
- Przyciski AI w całym panelu trzymają PRAWĄ krawędź (blok wyrównany `text-right`), a objaśnienia idą POD polem, nad przyciskiem.

## NASTĘPNY TEMAT
Wg [[priorities-launch-first]]: **Zwroty Fazy B i C** ([[legal-consumer-returns-withdrawal]]) — formularz zwrotu na tokenie + `returned_quantity`, potem sekcja „Zwroty" w panelach. Faza A (obowiązek informacyjny) zrobiona 2026-07-25. Relacja `Product::orderItems()`, której Faza B potrzebuje, powstała 2026-07-27.
Dalej: korespondencja seryjna (+ link wypisu) → płatności za pakiety i zakup z głównej (z dopłatą przy zmianie pakietu) → wysyłki → panel admina → wygaśnięcie pakietu.
