---
name: handoff-2026-07-20
description: "Handoff 2026-07-20: strona glowna oczyszczona z fejkow + realny cennik roczny; wymog dostepnosci produktow domkniety (komunikat w koszyku)."
metadata: 
  node_type: memory
  type: project
  originSessionId: ffdcbddc-37d0-49c6-ab31-8ec827d85dbc
  modified: 2026-07-20T20:27:34.886Z
---

**Sesja 2026-07-20 (z Michałem). Dwa tematy domknięte, oba scommitowane i wypchnięte na `main`.**

**1. Strona główna `welcome.blade.php` — wycięcie fejków + realny cennik** (commit `92321ec`):
- Usunięte zmyślone statystyki („1 200+ sklepów", „5 min", złe liczby produktów) → sekcja **Pakiety** czytana z `config('shop.packages')` (jedno źródło prawdy).
- Ceny **roczne** (750/1500 zł) z rozbiciem „to X zł/mies. — płacisz za 10 miesięcy, 2 gratis". Kram = 0 zł.
- Etykiety funkcji **zweryfikowane w kodzie**: Paynow / InPost / Fakturownia = działają; **kody rabatowe i newsletter oznaczone plakietką „wkrótce"** (`'soon' => true` w tablicy features — bo `discount_codes` i `bulk_mail` to na razie tylko flagi entitlements, brak implementacji).
- „Sklepy które mogłyby powstać" → „Dla kogo jest Kramio" (branże). Makieta hero → plakietka „Przykładowy sklep". Poprawki tekstów hero (podwójne „swój") + AI („korekta" nie „generowanie" — funkcja to `AiTextImprover`/„Popraw przez AI"). Zamknęło [[open-landing-fabricated-stats]].
- Uwaga: `DEEPSEEK_API_KEY` JEST ustawiony (pamięć „pusty" była nieaktualna) — AI działa.

**2. Dostępność produktów — domknięte** (commit `837bfba`): patrz [[stock-availability-verification]]. Dołożony komunikat korekty przy WYŚWIETLANIU koszyka (`CartService::reconcile()` → notices + baner w `livewire.cart`); reszta (atomowe zdjęcie+kolizje przy składaniu) była już zrobiona. 6 nowych testów.

**Nietknięte / następne w kolejce** (z [[plan-admin-panel-and-landing]] i indeksu):
- Landing można dalej podnosić: ikony zamiast emoji w sekcji Funkcje (kłóci się z „warm boutique"), CTA na karcie Kram, realny screenshot demo-sklepu.
- Otwarte duże: edytor uprawnień per-sklep (potwierdzić stan po commitach Fazy 3), kafelki SaaS na pulpicie admina (0/do billingu), zwroty 14 dni (plan zatwierdzony, w kolejce), Furgonetka+etykiety, link wypisu z mailingu.
- Gdy wdrozysz kody rabatowe / newsletter → usun `'soon' => true` z odpowiedniej linii w `welcome.blade.php` i plakietka „wkrótce" znika.
