---
name: handoff-2026-08-24
description: "Sesja 24.08.2026: awaria ekranu rejestracji klienta (Blade), mail do sprzedawcy, moduł korespondencji serwisowej, garda fabryk na produkcji, przegląd ekranów. 4 commity, 1693→1704 testy."
metadata: 
  node_type: memory
  type: project
  originSessionId: bf48269a-c52a-49c6-9a8e-6a7c2b21dfa6
  modified: 2026-08-24T09:44:16.277Z
---

**Sesja interwencyjna** — zaczęła się od alertu na Discordzie, nie od planu. **Cztery commity:** `7991538` (naprawa), `a1ad0e8` (wiadomość serwisowa), `ba2896a` (garda fabryk), `8f46f64` (przegląd ekranów). Testy 1693 → **1704**, zielone.

## Jak poszła sesja

Alert o 9:55: `unexpected token "endif"` na `szyteprzezluke.kramio.pl/rejestracja/potwierdzenie`. Przyczyna: `@if` przyklejone do słowa — [[blade-directive-glued-to-word-not-compiled]]. Ekran padał **od 13.07 dla każdego rejestrującego się klienta**; nikt się dotąd nie rejestrował.

Konto klientki mimo błędu założyło się poprawnie i zostało aktywowane 97 sekund później — padł tylko ekran po redirectcie. Sprzedawca `szyteprzezluke` (Łukasz Żołnierewicz, sklep założony 23.08) to najpewniej ta sama rodzina, więc świeży sprzedawca zobaczył 500 drugiego dnia. Mail przeprosinowy wysłany po akceptacji Rafała.

Ten mail nie dał się wysłać z panelu — sprzedawca nie ma zgody marketingowej, a dział „Wiadomości" bramkuje nią odbiorców. Stąd nowy moduł: [[plan-seller-service-notices]].

Potem Rafał zauważył w panelu konto „sprzedawca bez sklepu" — okazało się mieć hasło `password` z fabryki: [[gotcha-factory-on-production-db]]. Usunięte, garda dodana (DB_SECURITY Warstwa 7).

Na koniec przegląd, czy nie ma drugiego takiego błędu jak poranny. Nie ma.

## Dwa razy pierwsza hipoteza brzmiała groźniej niż prawda

1. Błąd rejestracji **wyglądał na losowy** (jedno zgłoszenie), a był systematyczny od sześciu tygodni.
2. Konto z Fakera **wyglądało na testy piszące do produkcyjnej bazy**, a było ręcznym wywołaniem w tinkerze.

W obu razach rozstrzygnął dopiero konkretny dowód: `Hash::check('password', …)` i skompilowanie widoku z gita. **Samo „sprawdziłem, jest ok" bez takiego dowodu niewiele znaczy.**

## Rafał sprostował 1× — trafnie

Napisałem w mailu do sprzedawcy „wczoraj o 9:55”; rejestracja była **dziś**, wczoraj powstał sklep. Przy okazji poprawki wyszło, że zdanie „koszyk i zamówienia działały normalnie” też było na wyrost — sklep ma 0 zamówień, więc nikt tego nie używał. **W mailach o cudzej wpadce data i zakres muszą być sprawdzone, nie przepisane z pamięci.** Wnioski: datę podawać bezwzględną (mail wysłany dzień później czyni „dziś” nieprawdą).

## Technika warta zapamiętania

Audyt **wszystkich** widoków naraz, niezależnie od pokrycia testami:

```bash
php artisan view:clear && php artisan view:cache
for f in storage/framework/views/*.php; do php -l "$f" >/dev/null || echo "BŁĄD: $f"; done
```

**Zawsze weryfikuj, że taki audyt cokolwiek wykrywa** — skompilowałem wersję widoku sprzed poprawki (`git show 7991538^:…` + `Blade::compileString`) i sprawdziłem, że wskazuje tamten błąd. Bez tego „zero błędów” znaczy tylko tyle, że narzędzie milczy.

## Stan na koniec

- **Moduł korespondencji serwisowej świadomie minimalny** — Rafał: *„wolałbym, by nie było nigdy używane, a jak się okaże, że będzie, to dodamy brakujące funkcje"*. Bez historii wysłanych, bez podglądu, bez wysyłki do wielu. **Nie planować tego jako zaległości.**
- Sprzedawców jest 6 (jeden sklep na każdego oprócz admina), klientów storefrontu 2. **Pierwsi prawdziwi ludzie już wchodzą** — stąd waga drobiazgów.
- Otwarte bez zmian: [[open-no-file-backups]] etap 2, [[plan-dev-environment]], test produkcyjny InPost, [[plan-seller-legal-templates]].
